#include <stdio.h>
main(){
float length,width;
printf("type length:");
    scanf("%f",&length);
printf("type width:");
    scanf("%f",&width);
float a=length*width;

printf("area of rectangle:%f",a);

}